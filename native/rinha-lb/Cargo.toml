[package]
name = "rinha_lb"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "rinha-lb"
path = "src/main.rs"

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2"
socket2 = { version = "0.5", features = ["all"] }

[profile.release]
lto = "fat"
codegen-units = 1
strip = true
opt-level = 3
panic = "abort"
overflow-checks = false
incremental = false
